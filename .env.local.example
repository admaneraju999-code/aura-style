# Copy this file to `.env.local` and fill in your database connection string.
# For local dev: get this from your Vercel Postgres / Neon dashboard.
# For Vercel deploy: you do NOT put secrets here — configure it in the
# Vercel dashboard under Settings > Environment Variables instead.
POSTGRES_URL=postgresql://user:password@host:port/database
